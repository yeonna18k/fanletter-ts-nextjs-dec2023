import React from "react";

const Detail: React.FC = (props) => {
  return (
    <>
      <div>Detail</div>
      {/* {props.params.id} 로 id값을 받아올 수 있음
      main에서 detail 페이지로 넘어가는 링크? a태그나 네비게이트 사용 시
      뒤에 id값을 params로 넘겨주면 될 듯*/}
    </>
  );
};

export default Detail;
